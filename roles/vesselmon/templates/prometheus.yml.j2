scrape_configs:
  - job_name:       'vesselmon'

    # Override the global default and scrape targets from this job every 5 seconds.
    scrape_interval: 5s

    static_configs:
      - targets: [{{ vesselmon_prometheus_targets | join(',') }}]
